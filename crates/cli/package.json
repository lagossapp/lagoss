{
  "name": "@lagoss/cli",
  "version": "0.9.1",
  "description": "CLI for Lagoss",
  "type": "module",
  "files": [
    "bin"
  ],
  "bin": {
    "lagoss": "./bin/run.js"
  },
  "scripts": {
    "build": "esbuild --outdir=bin/ --platform=node --packages=external --format=esm --bundle --minify lib/run.ts lib/install.ts && cargo build",
    "lint": "cargo clippy -- -Dwarnings --no-deps",
    "postinstall": "node bin/install.js"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "esbuild": "^0.18.7",
    "rimraf": "^5.0.7",
    "tar": "^7.4.0",
    "update-notifier": "^7.0.0"
  },
  "devDependencies": {
    "@types/update-notifier": "^6.0.8",
    "tsup": "^7.1.0"
  }
}
